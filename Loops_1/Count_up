The following code outputs a countdown from 10 to 1.

count = 10

until count == 0
  puts count
  count -= 1
end

Q: Modify the code so that it counts from 1 to 10 instead.

My A:

count = 1

until count > 10
  puts count
  count += 1
end

