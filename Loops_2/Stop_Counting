The method below counts from 0 to 4.

5.times do |index|
  # ...
end

Q: Modify the block so that it prints the current number and stops iterating when 
   the current number equals 2.

My A:

5.times do |index|
  puts index
  break if index == 2
end
